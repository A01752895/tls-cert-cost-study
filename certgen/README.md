# Certificate generation

This module generates real X.509 certificate chains for controlled TLS experiments.

Usage:
```bash
bash certgen/generate_certs.sh rsa 2
bash certgen/generate_certs.sh ecdsa 3
